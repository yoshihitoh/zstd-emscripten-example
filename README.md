# An example of Zstandard library on browser

## Setup

```bash
yarn install
```

## Run

run server
```bash
yarn run webpack
cd public
python -m SimpleHTTPServer
```

access: `http://localhost:8000`
